# Fullsend CodeRabbit fork pilot

This temporary, documentation-only change exists solely to trigger the Fullsend
workflow on the fork after CodeRabbit context integration was merged into the
fork's default branch. It must not be merged.
